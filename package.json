{
  "private": true,
  "workspaces": [
    "packages/frontend",
    "packages/server"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "dependencies": {
    "turbo": "^1.9.9"
  }
}
